# Finance tracker
A simple Python script to track expenses.
It prompts the user for an item, a category to assign it to, and its price.
It then stores the data in a .csv file for data retention and shows the accumulated total expense.
It's a basic CRUD application - well, at least the "C" and the "R" are there. The rest is a work in progress!
